module.exports = {
    LEADER_ELECTION_SERVICE: 'http://localhost:5000', // Address of the leader election service
    //DB_CONNECTION: 'postgresql://user:password@localhost/database', // Connection string for the database
    API_PORT: 3000, // Port for this server's API
};
